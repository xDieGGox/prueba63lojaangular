export class Nota{
    uid?: string;
    titulo?: string; //Aqui con el signo de pregunta, no es necesario definir la variable con algun valor y quedaria como valor undefined
    descripcion?: string;
    fecha?: string;
}